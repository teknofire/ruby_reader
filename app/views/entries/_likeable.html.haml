- if entry.likes.where(user: current_user).empty?
  = link_to like_entry_path(entry), method: :patch, class: 'pull-left likeable', remote: true do
    %i.icon-star-empty
- else
  = link_to unlike_entry_path(entry), method: :patch, class: 'pull-left likeable active', remote: true do
    %i.icon-star
